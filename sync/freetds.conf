[global]
    # 实测 SQL Server 2008 R2 (10.50.1600.1) 需使用 TDS 7.0
    tds version = 7.0
    # 客户端字符集
    client charset = UTF-8
    # 连接超时（秒）
    connect timeout = 10
    # 查询超时（秒）
    timeout = 60

[U8_SERVER]
    host = 192.168.1.135
    port = 1433
    tds version = 7.0
    client charset = UTF-8
